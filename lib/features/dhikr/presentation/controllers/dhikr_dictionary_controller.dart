// Re-export for clarity. The dictionary provider is defined alongside the
// counters controller because it's used by the home screen's Suggested
// section (which is itself derived from counters + dictionary).

export 'dhikr_counters_controller.dart' show dhikrDictionaryProvider;
